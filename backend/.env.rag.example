# RAG System Configuration - Environment Variables
# Copy this file to .env and customize the values as needed

# Database Configuration
DB_NAME=medixscan_db
DB_USER=postgres
DB_PASSWORD=password
DB_HOST=localhost
DB_PORT=5432

# OpenAI Configuration
OPENAI_API_KEY=your_openai_api_key_here

# RAG Content Sources Configuration
RAG_RADIOLOGY_BASE_URL=https://radiologyassistant.nl
RAG_RADIOLOGY_ENABLED=true
RAG_MAX_PAGES=50
RAG_REQUEST_DELAY=1.0
RAG_REQUEST_TIMEOUT=30
RAG_RETRY_ATTEMPTS=3
RAG_USER_AGENT=Medical Education Bot 1.0

# RAG Content Selectors (CSS selectors for web scraping)
RAG_CONTENT_SELECTOR=.content, .main-content, article, .post-content
RAG_TITLE_SELECTOR=h1, h2, .title
RAG_MEDICAL_SELECTOR=p, li, .description

# Medical Terms Configuration
RAG_MIN_WORD_LENGTH=3
RAG_MAX_WORD_LENGTH=50

# Cache Configuration
RAG_VOCAB_CACHE_TIMEOUT=3600        # 1 hour in seconds
RAG_CONTENT_CACHE_TIMEOUT=86400     # 24 hours in seconds
RAG_MAX_CACHE_SIZE=1000
RAG_CACHE_PREFIX=rag_

# Analysis Configuration
RAG_OPENAI_MODEL=gpt-3.5-turbo
RAG_MAX_TOKENS=1000
RAG_TEMPERATURE=0.3
RAG_CONFIDENCE_THRESHOLD=0.7
RAG_MAX_REPORT_LENGTH=10000
RAG_MIN_REPORT_LENGTH=50

# Performance Tuning
# Adjust these values based on your server capacity and requirements

# For high-volume deployments:
# RAG_MAX_PAGES=100
# RAG_REQUEST_DELAY=0.5
# RAG_VOCAB_CACHE_TIMEOUT=7200

# For conservative/low-resource environments:
# RAG_MAX_PAGES=25
# RAG_REQUEST_DELAY=2.0
# RAG_VOCAB_CACHE_TIMEOUT=1800

# Development vs Production
# In development, you might want faster updates:
# RAG_CONTENT_CACHE_TIMEOUT=1800  # 30 minutes

# In production, longer cache times are more efficient:
# RAG_CONTENT_CACHE_TIMEOUT=172800  # 48 hours
